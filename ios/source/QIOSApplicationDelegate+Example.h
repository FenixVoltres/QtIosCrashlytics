// Declaration only for compile process, the real class
// is in Qt plugins (Src/qtbase/src/plugins/platforms/ios)
@interface QIOSApplicationDelegate
@end

@interface QIOSApplicationDelegate (Example)

- (void) setupCrashlytics;

@end
